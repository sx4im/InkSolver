{
  "experimentalServices": {
    "frontend": {
      "routePrefix": "/",
      "framework": "nextjs",
      "buildCommand": "pnpm run build",
      "installCommand": "pnpm install"
    },
    "verifier": {
      "routePrefix": "/api/verifier",
      "framework": "fastapi",
      "installCommand": "pip install -r requirements.txt",
      "buildCommand": "echo 'No build step for verifier'",
      "rootDirectory": "services/verifier"
    }
  }
}
