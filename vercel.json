{
  "experimentalServices": {
    "frontend": {
      "routeProfile": "/",
      "framework": "nextjs",
      "buildCommand": "pnpm run build",
      "installCommand": "pnpm install",
      "outputDirectory": ".next"
    },
    "verifier": {
      "routeProfile": "/api/verifier",
      "framework": "fastapi",
      "installCommand": "pip install -r requirements.txt",
      "buildCommand": "echo 'No build step for verifier'",
      "rootDirectory": "services/verifier"
    }
  }
}
