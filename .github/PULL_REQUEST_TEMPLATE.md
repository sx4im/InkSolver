## What does this PR do?

<!-- One or two sentences. Link the issue it closes, e.g. "Closes #12". -->

## How was it tested?

- [ ] `pnpm typecheck && pnpm lint && pnpm build`
- [ ] `pnpm smoke:local` passes
- [ ] Playwright / pytest updated if behavior changed

## Screenshots

<!-- For UI changes, before/after screenshots (mobile too, if relevant). -->
